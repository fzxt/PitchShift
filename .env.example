# Optional. Used only by the local/nightly Python pipeline. Never sent to browsers.
DATABASE_URL=
# Data settings; all data stays within a single Statcast measurement era.
PITCHSHIFT_SEASON=2026
PITCHSHIFT_START_DATE=2026-07-01
